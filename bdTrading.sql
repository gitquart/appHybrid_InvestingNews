

CREATE TABLE tbControl (
        ID INT primary key,
        app varchar(50),
        limit_iteration int,
        lsControl varchar(50),
        noinfolimit int,
        page int,
        query varchar(50)
);